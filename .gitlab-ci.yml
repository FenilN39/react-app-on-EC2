stages:
  - login

login-to-ec2:
  image: ubuntu:latest
  stage: login
 
  script:
    - apt update
    - apt-get install -y openssh-server
    - chmod 400 $LOGIN_FILE
    - ssh -i "$LOGIN_FILE" $USER@$PASSWD "echo $(pwd)"
    - echo "login successful"

    - echo "into server"
    - exit
